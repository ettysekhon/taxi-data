[project]
name = "spark-batch"
version = "0.1.0"
description = "Batch processing with PySpark 4.x"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=4.0.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.0.0",
    "pandas>=2.0.0",
]
